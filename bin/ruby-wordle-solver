#!/usr/bin/env ruby

$LOAD_PATH << File.join( File.dirname(__FILE__), '..', 'lib')
$LOAD_PATH << File.join( File.dirname(__FILE__), '..', 'lib', 'ruby-wordle-solver')

require 'version'

if ARGV[0] == "-h" || ARGV[0] == "--help"
  puts "Ruby Wordle Solver"
  puts "See https://github.com/yohasebe/ruby-wordle-solver"
  exit
elsif ARGV[0] == "-v" || ARGV[0] == "--version"
  puts "Ruby Wordle Solver #{RubyWordleSolver::VERSION}"
  exit
end

require 'script-solver'
